"""Small committed reference values used by tests and validation scripts.

Only compact, provenance-tagged subsets of reference tables live here (a few
dozen numbers). Full datasets are acquired and cached by the external-data
layer (Stage 0, task DEV-003), never committed.
"""
